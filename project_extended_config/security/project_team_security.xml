<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
         <record id="project.project_public_members_rule" model="ir.rule">
            <field name="active" eval="False"/>
        </record>
        <!-- Record rule: Team members can only see projects where they are team members -->
        <record id="project_team_member_rule" model="ir.rule">
            <field name="name">Project Team Member Access</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="domain_force">[('team_member_ids', 'in', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_project_team_member'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Record rule: Team managers can see and manage projects in their teams -->
        <record id="project_team_manager_rule" model="ir.rule">
            <field name="name">Project Team Manager Access</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="domain_force">[('team_manager_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_project_team_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Record rule: Team admins can see and manage all projects -->
        <record id="project_team_admin_all_access_rule" model="ir.rule">
            <field name="name">Project Team Administrator All Access</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_project_team_admin'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo> 